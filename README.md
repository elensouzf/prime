# Prime

A simple program to print prime numbers using C.

## Building

Run `make` to compile the program.

## Usage

```
./prime <limit>
```

Replace `<limit>` with an integer to print all primes up to that number.

